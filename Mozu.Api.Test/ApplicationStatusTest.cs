using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Mozu.Api.Test
{
    [TestClass]
    public class ApplicationStatusTest : BaseTest
    {
        [TestMethod]
        public void SetInitializedTest()
        {
            /*var mzClient = Operations.Platform.Tenants.GetTenant(3679);
            var tenant = mzClient.WithBaseAddress(BaseUrl).Execute().Result();

            var tenantDomain = "Http://" + tenant.Domain.DomainName;

            var mzClient1 = Applicationstatus.SetApplicationStatusInitialized(false);

            mzClient1.WithBaseAddress(tenantDomain)
                    .WithContext(3679)
                    .Execute();*/

        }
    }
}
